apiVersion: v1
kind: Service
metadata:
    name: mssql-load-balancer
spec:
    type: LoadBalancer
    selector:
        app: mssql
    ports:
      - protocol: TCP
        port: 1433
        targetPort: 1433